{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/TFNExemptionType",
  "title": "TFNExemptionType",
  "type": "string",
  "enum": [
    "NOTQUOTED",
    "PENDING",
    "PENSIONER",
    "UNDER18"
  ]
}